<!DOCTYPE html>
<html lang="ko">
  <head>
    <title><%= content_for(:title) || "관리자 - L-BIZ Partners" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="robots" content="noindex, nofollow">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen bg-gray-100">
    <div class="flex">
      <!-- Sidebar -->
      <aside class="w-64 bg-gray-900 min-h-screen fixed">
        <div class="p-4">
          <!-- Logo -->
          <div class="flex items-center mb-8">
            <span class="text-xl font-bold text-blue-400">L-BIZ</span>
            <span class="text-xl font-light text-gray-300 ml-1">Admin</span>
          </div>

          <!-- Navigation -->
          <nav class="space-y-2">
            <%= link_to admin_root_path, class: "flex items-center px-4 py-2 text-gray-300 hover:bg-gray-800 rounded-lg transition-colors #{controller_name == 'portfolios' && action_name == 'index' ? 'bg-gray-800' : ''}" do %>
              <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
              </svg>
              대시보드
            <% end %>

            <%= link_to admin_portfolios_path, class: "flex items-center px-4 py-2 text-gray-300 hover:bg-gray-800 rounded-lg transition-colors #{controller_name == 'portfolios' ? 'bg-gray-800' : ''}" do %>
              <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10" />
              </svg>
              포트폴리오
            <% end %>

            <%= link_to admin_contacts_path, class: "flex items-center px-4 py-2 text-gray-300 hover:bg-gray-800 rounded-lg transition-colors #{controller_name == 'contacts' ? 'bg-gray-800' : ''}" do %>
              <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
              </svg>
              문의 관리
            <% end %>

            <%= link_to admin_blog_index_path, class: "flex items-center px-4 py-2 text-gray-300 hover:bg-gray-800 rounded-lg transition-colors #{controller_name == 'blog' ? 'bg-gray-800' : ''}" do %>
              <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 20H5a2 2 0 01-2-2V6a2 2 0 012-2h10a2 2 0 012 2v1m2 13a2 2 0 01-2-2V7m2 13a2 2 0 002-2V9.5a2.5 2.5 0 00-2.5-2.5H15" />
              </svg>
              블로그
            <% end %>
          </nav>
        </div>

        <!-- Logout -->
        <div class="absolute bottom-0 w-full p-4 border-t border-gray-800">
          <%= link_to admin_session_path, data: { turbo_method: :delete }, class: "flex items-center px-4 py-2 text-gray-400 hover:text-white transition-colors" do %>
            <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
            </svg>
            로그아웃
          <% end %>
        </div>
      </aside>

      <!-- Main Content -->
      <div class="flex-1 ml-64">
        <!-- Top Bar -->
        <header class="bg-white shadow-sm">
          <div class="px-6 py-4 flex justify-between items-center">
            <h1 class="text-xl font-semibold text-gray-800"><%= content_for(:page_title) || "대시보드" %></h1>
            <div class="flex items-center space-x-4">
              <%= link_to root_path, target: "_blank", class: "text-sm text-gray-500 hover:text-blue-600" do %>
                <svg class="w-5 h-5 inline mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
                </svg>
                사이트 보기
              <% end %>
            </div>
          </div>
        </header>

        <!-- Flash Messages -->
        <% if flash[:notice] %>
          <div class="mx-6 mt-4 bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded-lg">
            <%= flash[:notice] %>
          </div>
        <% end %>
        <% if flash[:alert] %>
          <div class="mx-6 mt-4 bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded-lg">
            <%= flash[:alert] %>
          </div>
        <% end %>

        <!-- Page Content -->
        <main class="p-6">
          <%= yield %>
        </main>
      </div>
    </div>
  </body>
</html>
